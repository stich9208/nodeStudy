export const home = (req, res) => {
  res.send("<h1>HOME!</h1>");
};

export const join = (req, res) => {
  res.send("<h1>JOIN!</h1>");
};

export const detail = (req, res) => {
  res.send("<h1>USER DETAIL!</h1>");
};
